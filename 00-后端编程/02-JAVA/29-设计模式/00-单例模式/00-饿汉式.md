## 饿汉式

```java
public class Test {

  // 1. 私有化的构造器
  private Test() {
  }

  // 2. 内部创建类的对象
  // 3. 要求此对象也必须声明为静态的
  private static Test instance = new Test();

  // 3. 提供公共的静态的方法, 返回类对象
  public static Test getInstance() {
    return instance;
  }
}
```