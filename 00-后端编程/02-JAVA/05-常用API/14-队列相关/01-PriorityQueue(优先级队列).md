

```java

boolean add(E element)：此方法将指定的元素插入此优先级队列。

public remove()：此方法从此队列中删除指定元素的单个实例（如果存在）

public poll()：此方法检索并删除此队列的头部，如果此队列为空，则返回null。

public peek()：此方法检索但不删除此队列的头部，如果此队列为空，则返回null。

Iterator iterator()：返回此队列中元素的迭代器。

boolean contains(Object o)：如果此队列包含指定的元素，则此方法返回true

void clear()：此方法用于删除优先级队列的所有内容。

boolean offer(E e)：此方法用于将特定元素插入优先级队列。

int size()：该方法用于返回集合中存在的元素数。

toArray()： 此方法用于返回包含此队列中所有元素的数组。

Comparator comparator(): ：该方法用于返回可用于对队列元素进行排序的比较器。
```











